# database-service.yaml
# Exposes PostgreSQL inside the cluster so backend can reach it

apiVersion: v1
kind: Service
metadata:
  name: database
spec:
  selector:
    app: database
  ports:
    - port: 5432
      targetPort: 5432